import time #Этот модуль нужен для работы со временем

from selenium import webdriver  #позволяет управлять браузером программно: открывать сайты, кликать, вводить текст и т.д. это основной инструмент Selenium для взаимодействия с браузерами (Chrome, Firefox и др.).
from selenium.webdriver.chrome.options import Options # Импорт класса Options для браузера Chrome С помощью Options мы настраиваем поведение Chrome перед его запуском. сюда можно добавить настройки, такие как работа через прокси, запуск без интерфейса и др.

PROXY_SERVER = "23.237.210.82:80"  # Прокси-сервер HTTPS
#PROXY = "username:password@37.19.220.129:8443" # Прокси-сервер с авторизацией


options = Options() #Это заготовка для конфигурации браузера. ❗ Зачем: сейчас мы добавим туда прокси.
options.add_argument(f"--proxy-server={PROXY_SERVER}") #Параметр --proxy-server=... указывает Chrome, что все соединения нужно направлять через заданный прокси. ❗ Зачем: чтобы браузер открывал сайты через сторонний IP, указанный в PROXY_SERVER.
driver = webdriver.Chrome(options=options)  #🔹 Создаем экземпляр браузера Chrome с заданными настройками (включая прокси) driver — это объект, через который мы управляем браузером. ❗ Зачем: запускаем браузер, готовый к работе через прокси.

driver.get("https://whoer.net/") # Проверяем IP-адрес 89.46.238.156 сменился на 23.237.210.82 

time.sleep(5)


Сайты для проверки IP 
https://2ip.ru	Показывает IP, страну, браузер, DNS и т.д.
https://whoer.net	Более подробный анализ IP, DNS, WebRTC, анонимности
https://ipinfo.io	Чисто IP и информация о провайдере, есть API
https://ipleak.net	Продвинутый тест утечек IP через WebRTC и DNS
https://whatismyipaddress.com	Классический инструмент для определения IP и местоположения

